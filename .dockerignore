.git
.github
.venv
.venv-wsl
__pycache__
*.pyc
*.pyo
*.pyd
*.swp
*.swo
notebooks/
tex/
mlruns/
terraform/

# Large local data trees are typically mounted, not baked
# Keep processed samples if needed by UI, skip raw datasets by default
data/raw/

# Artefacts ML (modèles, rapports) : gérés par DVC/S3, pas baked dans l'image
artifacts/

# Front : les dépendances (~400 Mo) sont réinstallées PAR l'image de build
# (npm ci). Les envoyer dans le contexte Docker ferait perdre une minute à
# chaque construction, pour un contenu aussitôt jeté.
frontend/node_modules/
frontend/dist/
frontend/.angular/
