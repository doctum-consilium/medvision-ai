# Dépendances d'ENTRAÎNEMENT TensorFlow — pipeline DVC (dvc repro).
# PAS utilisé dans l'image Docker de production (l'inférence passe par onnxruntime).
#
# Environnement dédié TensorFlow (cu12). NE PAS y installer PyTorch : TF (NCCL cu12 2.19)
# et torch (NCCL cu13 2.29) partagent le soname libnccl.so.2 et se cassent mutuellement.
# Les modèles PyTorch/vision ont leur propre env → requirements-torch.txt.
#
# Usage (Python 3.10–3.12, venv ou conda) :
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-train.txt
#   source scripts/gpu_env.sh            # rend les libs CUDA pip visibles à TF 2.16
#   python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"

# Hérite de toutes les deps d'inférence
-r requirements.txt

# TensorFlow + Keras (versions qui ont produit les modèles actuels)
# [and-cuda] = libs CUDA/cuDNN 8 groupées → GPU sans installation système.
# Sans cet extra, TF retombe sur CPU ("Skipping registering GPU devices").
tensorflow[and-cuda]==2.16.1; python_version < "3.13"
keras==3.13.2

# Conversion ONNX (keras → onnx).
# onnx < 1.18 : les versions ≥ 1.18 exigent ml_dtypes ≥ 0.4 (float4_e2m1fn),
# incompatible avec le ml_dtypes 0.3.x épinglé par TensorFlow 2.16.
tf2onnx>=1.16
onnx>=1.16,<1.18

# Medical imaging
nibabel>=5.0

# Notebooks
jupyterlab==4.3.5
notebook==7.3.2
ipykernel==6.29.5

# Téléchargement datasets
kaggle>=1.7.4.5
